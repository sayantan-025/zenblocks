---
title: "Shuffle"
description: "A cyberpunk-inspired text reveal effect with random character decryption."
icon: RefreshCw
---

import { Shuffle, ShuffleDemo } from "@/components/zenblocks/shuffle";
import { Preview } from "@/components/mdx/preview";

## Overview

**Shuffle** creates a dramatic text reveal animation where characters rapidly cycle through random letters before settling on the final text. This effect is perfect for hero headlines, futuristic UI elements, and data visualization labels.

---

## Features

-   **Cyberpunk Aesthetic**: Random character cycling creates a "decryption" feel.
-   **Configurable Speed**: Adjust animation duration and cycle rate.
-   **Zero Dependencies**: Pure React implementation (no heavy animation libraries required).
-   **Auto-Play**: Starts animation on mount or prop change.

---

## Preview

<Preview link="shuffle">
  <div className="relative w-full h-[200px] rounded-xl overflow-hidden border border-zinc-800">
    <ShuffleDemo />
  </div>
</Preview>

---

## Installation

### Method 1: CLI (Recommended)

Install via the ZenBlocks CLI to automatically handle dependencies:

```bash
npx shadcn@latest add https://zenblocks-three.vercel.app/r/shuffle.json
```

### Method 2: Manual

1.  **Copy the Source Code**

    Copy the code below into `components/zenblocks/shuffle.tsx`.

    <details>
    <summary>Click to expand source</summary>

    ```tsx
    "use client";

    import React, { useState, useEffect } from "react";

    interface ShuffleProps {
      text: string;
      className?: string;
      duration?: number;
    }

    export const Shuffle: React.FC<ShuffleProps> = ({ text, className }) => {
      const [displayText, setDisplayText] = useState(text);

      useEffect(() => {
        let iteration = 0;
        const interval = setInterval(() => {
          setDisplayText((prev) =>
            prev
              .split("")
              .map((letter, index) => {
                if (index < iteration) {
                  return text[index];
                }
                return String.fromCharCode(65 + Math.floor(Math.random() * 26));
              })
              .join("")
          );

          if (iteration >= text.length) {
            clearInterval(interval);
          }
          iteration += 1 / 3;
        }, 30);

        return () => clearInterval(interval);
      }, [text]);

    return <span className={className}>{displayText}</span>;
    };

    export function ShuffleDemo() {
      return (
        <div className="flex items-center justify-center h-full w-full bg-transparent p-8">
          <div className="inline-flex items-center gap-2 rounded-full border border-zinc-200 bg-white dark:bg-zinc-900 px-3 py-1 text-xs font-semibold text-zinc-600 dark:text-zinc-400 shadow-sm dark:border-zinc-800">
            <span className="flex h-1.5 w-1.5 rounded-full bg-emerald-500 animate-pulse"></span>
            <Shuffle text="v2.0 Now Available" />
          </div>
        </div>
      );
    }
    ```

    </details>

---

## Usage

### Basic Implementation

Import the component and pass the text you want to animate:

```tsx
import { Shuffle } from "@/components/zenblocks/shuffle";

export default function HeroSection() {
  return (
    <h1 className="text-4xl text-white">
      <Shuffle text="WELCOME FUTURE" />
    </h1>
  );
}
```

### Styling

The component accepts standard `className` props for styling:

```tsx
<Shuffle 
    text="ACCESS GRANTED" 
    className="text-emerald-500 font-mono tracking-widest"
/>
```

---

### Badge Implementation (Live Demo Style)

To recreate the badge style seen in the Hero section:

```tsx
import { Shuffle } from "@/components/zenblocks/shuffle";

export function Badge() {
  return (
    <div className="inline-flex items-center gap-2 rounded-full border border-zinc-200 bg-white dark:bg-zinc-900 px-3 py-1 text-xs font-medium text-zinc-600 dark:text-zinc-400">
      <span className="flex h-1.5 w-1.5 rounded-full bg-emerald-500 animate-pulse" />
      <Shuffle text="v2.0 Now Available" />
    </div>
  );
}
```

### Configuration & Customization

### Animation Speed
The shuffle effect runs on a set interval (currently 30ms per frame) defined internally. The duration is determined by the length of the string; standardizing roughly 3 frames per character reveal.

### Typography
Since this component renders raw text, it inherits all font properties (family, weight, size, color) from its parent or the `className` prop. It works exceptionally well with **Monospaced** fonts.

---

## API Reference

### Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `text` | `string` | - | The final text to display. |
| `className` | `string` | - | Optional CSS classes for styling. |
| `duration` | `number` | - | *Reserved for future implementation of custom timing.* |
