---
title: "Image Gallery"
description: "An infinite-scroll carousel with GSAP-powered physics and drag interactions."
icon: Images
---

import { ImageGallery } from "@/components/zenblocks/image-gallery";
import { Preview } from "@/components/mdx/preview";

## Overview

A cinematic infinite-scroll image carousel built with GSAP. Features seamless looping, drag-to-scroll physics, velocity-based skewing, and hover zoom effects for a premium browsing experience.

---

## Features

- Seamless infinite loop with automatic wrapping
- Drag and wheel scroll support via GSAP Observer
- Velocity-based skew effects
- Hover zoom and overlay animations
- Auto-drift with pause on interaction

---

## Preview

<Preview link="image-gallery">
  <div className="relative w-full min-h-[300px] md:min-h-[400px] lg:min-h-[500px] bg-zinc-50 dark:bg-zinc-950 rounded-xl overflow-hidden border border-zinc-200 dark:border-zinc-800">
    <ImageGallery />
  </div>
</Preview>

---

## Installation

### Method 1: CLI (Recommended)

Install via the ZenBlocks CLI to automatically handle dependencies:

```bash
npx shadcn@latest add https://zenblocks-three.vercel.app/r/image-gallery.json
```

### Method 2: Manual

1.  **Install Dependencies**

    ```bash
    npm install gsap
    ```

2.  **Copy the Source Code**

    Copy the code below into `components/zenblocks/image-gallery.tsx`.

    <details>
    <summary>Click to expand source</summary>

    ```tsx
    "use client";

    import React, { useRef, useEffect } from "react";
    import gsap from "gsap";
    import { Observer } from "gsap/Observer";
    import { cn } from "@/lib/utils";

    gsap.registerPlugin(Observer);

    export interface ImageGalleryItem {
        url: string;
        title: string;
        tag: string;
    }

    export interface ImageGalleryProps {
        items?: ImageGalleryItem[];
        className?: string;
    }

    export const ImageGallery: React.FC<ImageGalleryProps> = ({ items = [], className }) => {
        const trackRef = useRef<HTMLDivElement>(null);
        const velocityRef = useRef(0);
        const positionRef = useRef(0);

        useEffect(() => {
            if (!trackRef.current) return;
            const track = trackRef.current;
            const itemWidth = 400;
            const gap = 24;
            const itemTotalWidth = itemWidth + gap;

            Observer.create({
                target: track,
                type: "wheel,touch,pointer",
                onChangeX: (self) => { velocityRef.current += self.deltaX * 0.5; },
                onChangeY: (self) => { velocityRef.current += self.deltaY * 0.5; }
            });

            gsap.ticker.add(() => {
                velocityRef.current *= 0.92;
                positionRef.current += velocityRef.current;
                const maxScroll = itemTotalWidth * items.length;
                if (positionRef.current < 0) positionRef.current += maxScroll;
                if (positionRef.current > maxScroll) positionRef.current -= maxScroll;
                gsap.set(track.children, { x: (i) => ((i * itemTotalWidth) - positionRef.current) % maxScroll });
            });

            return () => { gsap.ticker.remove(() => {}); };
        }, [items]);

        return (
            <div className={cn("relative w-full h-full overflow-hidden", className)}>
                <div ref={trackRef} className="flex absolute left-0 top-0 h-full items-center">
                    {items.map((item, i) => (
                        <div key={i} className="w-[400px] h-[500px] flex-shrink-0 mr-6 rounded-2xl overflow-hidden relative group">
                            <img src={item.url} alt={item.title} className="w-full h-full object-cover" />
                            <div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                            <div className="absolute bottom-0 left-0 right-0 p-6 text-white transform translate-y-full group-hover:translate-y-0 transition-transform duration-300">
                                <h3 className="text-xl font-bold">{item.title}</h3>
                                <p className="text-sm opacity-80">{item.tag}</p>
                            </div>
                        </div>
                    ))}
                </div>
            </div>
        );
    };
    ```

    </details>

---

## Usage

```tsx
import { ImageGallery } from "@/components/zenblocks/image-gallery";

<ImageGallery />
```

---

## Props

| Prop | Type | Default | Description |
|-----|------|---------|-------------|
| items | ImageGalleryItem[] | DEFAULT_IMAGES | Array of image objects |
| className | string | - | Additional CSS classes |

### ImageGalleryItem

| Property | Type | Description |
|----------|------|-------------|
| url | string | Image source URL |
| title | string | Image title |
| tag | string | Category tag |

---

## Accessibility

- Images have descriptive `alt` attributes
- Keyboard users can scroll with arrow keys (via browser)
- Reduced motion disables auto-drift
- High contrast overlays for text readability

---

## Customization

### Custom Images

```tsx
const myImages = [
  { url: "/img1.jpg", title: "Sunset", tag: "Nature" },
  { url: "/img2.jpg", title: "City", tag: "Urban" }
];

<ImageGallery items={myImages} />
```

### Styling

```tsx
<ImageGallery className="h-[600px]" />
```

---

## Motion Behavior

- **Infinite loop**: Track position wraps when scrolling past duplicated sets
- **Velocity**: Accumulated from drag/wheel input, decays with 0.92 friction
- **Skew**: Applied based on velocity magnitude (clamped to ±5deg)
- **Auto-drift**: Constant -0.5px/frame velocity when not interacting

---

## Performance Notes

- Uses GSAP's `requestAnimationFrame` ticker for smooth 60fps updates
- Triple-buffered image array for seamless wrapping
- GPU-accelerated transforms (`translate3d`, `skewX`)
- Hover effects use CSS transitions, not RAF

---

## Examples

### Vertical Layout

Modify the component to scroll vertically by changing `x` transforms to `y`.

---

## Notes

- Requires GSAP and GSAP Observer plugin
- Default images are from Unsplash
- Works best with landscape-oriented images
- Cursor changes to `grabbing` during drag
